{
  "name": "openmerch-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Open-source self-hosted product customization platform for ecommerce",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "rebuild:editor": "pnpm --filter @openmerch/core build && pnpm --filter @openmerch/editor build && pnpm --filter @openmerch/demo build",
    "rebuild:admin": "pnpm --filter @openmerch/admin build",
    "rebuild:all": "pnpm --filter @openmerch/core build && pnpm --filter @openmerch/editor build && pnpm --filter @openmerch/demo build && pnpm --filter @openmerch/admin build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "eslint": "^9.18.0",
    "prettier": "^3.4.2",
    "turbo": "^2.4.4",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.21.0",
    "vitest": "^3.0.4"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0"
  }
}
